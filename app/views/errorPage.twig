{% include "header.twig" %}
<style>
.page-wrap {
    min-height: 100vh;
}
body {
    background: #dedede;
}

</style>
<div class="page-wrap d-flex flex-row align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-12 text-center">
                {% if errorCode %}   
                    <span class="display-1 d-block">{{errorCode}}</span>
                {% endif %}
                <div class="mb-4 lead">{{errorMessage}}</div>
                <a href="/home" class="btn text-black">Back to Home</a>
            </div>
        </div>
    </div>
</div>


{% include "footer.twig" %}